# Dev/test-only dependencies. Kept out of requirements.txt so the deployed
# Streamlit app stays lean. Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
httpx==0.28.1        # FastAPI TestClient (bdapps sidecar tests)
