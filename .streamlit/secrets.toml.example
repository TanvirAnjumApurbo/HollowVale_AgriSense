OPENAI_API_KEY = "sk-..."

# Optional fallback provider if OpenAI credit runs low; only used if LLM_PROVIDER=groq
# GROQ_API_KEY = "gsk_..."

# Optional, only needed once Tier 2 bdapps CaaS is wired in
# BDAPPS_APPLICATION_ID = "APP_000000"
# BDAPPS_PASSWORD = "..."
